sharding:
  jdbc:
    config:
      orchestration:
        name: demo_yaml_ds_sharding_ms
        overwrite: false
        type: sharding
        zookeeper:
          namespace: orchestration-yaml-demo
          server-lists: localhost:2181
     